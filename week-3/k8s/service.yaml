apiVersion: v1
kind: Service
metadata:
  name: nginx-app-lb
  namespace: dev
  annotations:
    oci.oraclecloud.com/load-balancer-type: "lb"
spec:
  type: LoadBalancer
  selector:
    app: nginx-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
