apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-data
  namespace: dev
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: oci-bv
  resources:
    requests:
      storage: 50Gi
